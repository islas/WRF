on :
  workflow_call :
    inputs :
      # Label control
      label : 
        required : true
        type     : string
      name     :
        required : true
        type     : string
      event_name :
        required : true
        type     : string
      event_number: 
        required : true
        type     : string
      event_label: 
        required : true
        type     : string
      ref        :
        required : true
        type     : string
      sha        :
        required : true
        type     : string

      # SANE Workflow
      paths:
        required : true
        type     : string
      dry_run:
        required : false
        type     : boolean
        default  : false
      host:
        required : false
        type     : string
      actions:
        required : false
        type     : string
        default  : '[]'
      actions_filter:
        required : false
        type     : string
      args     :
        required : false
        type     : string
      patch:
        required : false
        type     : string
      upload :
        required : false
        type     : string
        default  : 'on_failure'
      cache:
        required : false
        type     : boolean
      cache_id:
        required : false
        type     : string
      venv:
        required : false
        type     : string
        default  : './venv/sane'
      python_dependencies:
        required : false
        type     : string
        default  : '[]'
      upgrade:
        required : false
        type     : boolean
        default  : true

      # Skip known test failures (use cautiously)
      known_test_errors:
        required : false
        type     : string
        default  : '[]'

jobs:
  labeled_test:
    # Is 5 days a reasonable wait time for testing?
    timeout-minutes: 7200
    name: Run ${{ inputs.name }} on ${{ inputs.host }}
    runs-on: ${{ inputs.host }}
    steps:
    # Don't use gh checks as they are woefully underdeveloped as a feature leading
    # to confusing UI and misplaced metrics
    # https://github.com/orgs/community/discussions/24616
    - name: Set pending status
      id: check_run_start
      uses: actions/github-script@v8
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          github.rest.repos.createCommitStatus({
            owner:      context.repo.owner,
            repo:       context.repo.repo,
            sha:        '${{ inputs.event_name == 'push' && github.sha || inputs.sha }}',
            target_url: '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}',
            description: '${{ inputs.name }}',
            context:     '${{ inputs.host }}/${{ inputs.label }}',
            state:      'pending'
          })

    - uses: actions/checkout@v6
      with:
        submodules: true
        ref: ${{ inputs.event_name == 'push' && github.ref || inputs.ref }}

    - uses: islas/sane-workflows-action@9f9a83951d82f73ea2525fef7e2470b7ec297aa5
      name: ${{ inputs.name }}
      id: sane_workflows
      continue-on-error: ${{ inputs.known_test_errors != '[]' }}
      with:
        id       : ${{ inputs.label }}
        paths    : ${{ inputs.paths }}
        dry_run  : ${{ inputs.dry_run }}
        host     : ${{ inputs.host }}
        actions  : ${{ inputs.actions }}
        actions_filter : ${{ inputs.actions_filter }}
        args     : ${{ inputs.args }}
        patch    : ${{ inputs.patch }}
        upload   : ${{ inputs.upload }}
        cache    : ${{ inputs.cache }}
        cache_id : ${{ inputs.cache_id }}
        venv     : ${{ inputs.venv }}
        python_dependencies : ${{ inputs.python_dependencies }}
        upgrade  : ${{ inputs.upgrade }}
        pre      : true

    - if: ${{ inputs.known_test_errors != '[]' }}
      name: Filter known test errors
      shell: python
      run: |
        import json
        import re
        from pathlib import Path

        workflow = "${{ steps.sane_workflows.outputs.save_location }}/orchestrator.json"
        actions  = json.loads( Path( workflow ).read_text() )["actions"]

        known_test_errors = [ re.compile(s) for s in json.loads( """${{ inputs.known_test_errors }}""" ) ]

        filtered = [ s for s in actions if not any( r.search(s) for r in known_test_errors ) ]

        if len( filtered ) > 0:
          print( f"::error::Workflow contains unignored Action errors" )
          exit( 1 )

    - name: Set completed status
      if: ${{ always() }}
      uses: actions/github-script@v8
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          github.rest.repos.createCommitStatus({
            owner:        context.repo.owner,
            repo:         context.repo.repo,
            sha:          '${{ inputs.event_name == 'push' && github.sha || inputs.sha }}',
            target_url:   '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}',
            description:  '${{ inputs.name }}',
            context:      '${{ inputs.host }}/${{ inputs.label }}',
            state:        '${{ job.status == 'success' && 'success' || 'failure' }}'
          })

    - name : Remove '${{ inputs.label }}' label
      if : ${{ always() && inputs.event_label == inputs.label }}
      env:
        PR_NUMBER: ${{ inputs.event_number }}
      run: |
        curl \
          -X DELETE \
          -H "Accept: application/vnd.github.v3+json" \
          -H 'Authorization: token ${{ github.token }}' \
          https://api.github.com/repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/labels/${{ inputs.label }}
